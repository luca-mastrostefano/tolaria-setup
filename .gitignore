# Nothing to ignore in the profile repo itself. (`.claude/settings.local.json` used to be
# shipped as a template and re-included here; it is now declared via `claude_settings` in
# profile.json, which the engine merges into the settings.json it generates.)
