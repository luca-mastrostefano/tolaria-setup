# {{ project_name }} — Key Abstractions

> The nouns of the codebase: the handful of concepts everything else is built from. If a
> change redefines one of these, it needs an ADR and an update here in the same commit.

## Core concepts

_TODO: one subsection per abstraction — definition, invariants, where it's implemented,
what depends on it._

## Naming conventions

_TODO: the vocabulary rules that keep code and docs speaking the same language._
