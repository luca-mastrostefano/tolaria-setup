# {{ project_name }} — Architecture

> Living document: reflects the **current** state (active ADRs only). Update it in the same
> commit as any structural change — new component, data-model change, new integration.
> The decision *history* lives in [`docs/adr/`](./adr/README.md).

## System overview

_TODO: one diagram (prefer Mermaid) + a paragraph: the major components and how data flows
between them._

## Components

_TODO: one subsection per major component — what it owns, what it exposes, what it must not
know about._

## Data model

_TODO: the core entities and where they live (storage, formats, source of truth)._

## Key decisions in force

_TODO: link the active ADRs that shape the current architecture, one line each._
